# Bat

When Bat is installed, interactive fish defines `cat` as `bat --style plain` and `bcat` as `bat`. Plain styling removes decorations but does not mean every other Bat behavior is disabled.

Use `command cat` when you specifically need the system cat command. These aliases are not initialized in noninteractive shells. No separate Bat configuration file is included in this repository.
